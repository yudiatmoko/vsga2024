/* *    Hi, Code Enthusiast! *    https://github.com/yudiatmoko *//* *    Hi, Code Enthusiast! *    https://github.com/yudiatmoko */package com.iyam.myvsgaproject2.data;import android.content.ContentValues;import android.content.Context;import android.database.Cursor;import android.database.sqlite.SQLiteDatabase;import android.database.sqlite.SQLiteOpenHelper;import androidx.annotation.Nullable;import com.iyam.myvsgaproject2.model.Contact;import java.util.ArrayList;public class DatabaseHelper extends SQLiteOpenHelper {    public DatabaseHelper(@Nullable Context context) {        super(context, "VSGA Database", null, 1);    }    @Override    public void onCreate(SQLiteDatabase sqLiteDatabase) {        String CREATE_TABLE = """            CREATE TABLE Contact (                Nama TEXT,                Alamat TEXT,                id INTEGER PRIMARY KEY AUTOINCREMENT            );        """;        sqLiteDatabase.execSQL(CREATE_TABLE);    }    @Override    public void onUpgrade(SQLiteDatabase sqLiteDatabase, int i, int i1) {    }    public void addContact(String name, String address){        SQLiteDatabase db = this.getWritableDatabase();        ContentValues contentValues = new ContentValues();        contentValues.put("Nama", name);        contentValues.put("Alamat", address);        db.insert("Contact", null, contentValues);        db.close();    }    public ArrayList<Contact> getData(){        String query = "SELECT * FROM Contact";        SQLiteDatabase db = this.getReadableDatabase();        Cursor cursor = db.rawQuery(query, null);        ArrayList<Contact> data = new ArrayList<>();        if (cursor.moveToFirst()){            do {                String name = cursor.getString(cursor.getColumnIndexOrThrow("Nama"));                String address = cursor.getString(cursor.getColumnIndexOrThrow("Alamat"));                data.add(new Contact(name, address));            } while (cursor.moveToNext());        }        return data;    }    public void deleteById(String nama){        SQLiteDatabase db = this.getWritableDatabase();        db.delete("Contact", "Nama = ?", new String[]{String.valueOf(nama)});        db.close();    }}