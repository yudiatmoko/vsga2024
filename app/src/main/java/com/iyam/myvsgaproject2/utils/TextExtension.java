/* *    Hi, Code Enthusiast! *    https://github.com/yudiatmoko */package com.iyam.myvsgaproject2.utils;import android.text.SpannableStringBuilder;import android.text.Spanned;import android.text.TextPaint;import android.text.method.LinkMovementMethod;import android.text.style.ClickableSpan;import android.view.View;import android.widget.TextView;import androidx.annotation.NonNull;import androidx.core.content.ContextCompat;import com.iyam.myvsgaproject2.R;public class TextExtension {    public static void highLightWord(TextView textView, String word, Runnable onClick) {        SpannableStringBuilder ssBuilder = new SpannableStringBuilder(textView.getText());        ClickableSpan clickableSpan = new ClickableSpan() {            @Override            public void onClick(@NonNull View view) {                onClick.run();            }            @Override            public void updateDrawState(@NonNull TextPaint ds) {                super.updateDrawState(ds);                ds.setColor(ContextCompat.getColor(textView.getContext(), R.color.Crayola));                ds.setUnderlineText(false);            }        };        int startIndex = textView.getText().toString().indexOf(word);        if (startIndex != -1) {            int endIndex = startIndex + word.length();            ssBuilder.setSpan(clickableSpan, startIndex, endIndex, Spanned.SPAN_EXCLUSIVE_EXCLUSIVE);            textView.setText(ssBuilder);            textView.setMovementMethod(LinkMovementMethod.getInstance());        }    }}